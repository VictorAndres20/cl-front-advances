export const validateEnterprise = (body) => {
    const { name } = body;
    if(name === undefined || name === '') throw new Error(`Nombre vacío`);
}

export const transformEntityEnterprise = (entity) => {
    let newEnt = {...entity}
    return newEnt;
}

export const buildEmptyEnterprise = () => {
    return {
        name: '',
        address: '',
    };
}



/** Generated by https://github.com/VictorAndres20 code generator for database, NestJS, React */